# Manual Test Report

**Date (YYYY-MM-DD):** 2026-02-08  
**Tester:** Aryan Banerjee  

**Test Name:** User `/me` routes — Auth vs No-Auth (GET, PUT, DELETE) in Postman  

**Test Steps (summary):**
1. Started backend + DB, ensured a test user existed, and obtained a valid JWT for that user.
2. In Postman, set `Authorization: Bearer <JWT>` for the “auth” requests and removed auth for the “no auth” requests.
3. Ran these requests in order:
   - GET Me
   - GET Me With No Auth
   - PUT Change User info
   - PUT Change User info with No Auth
   - DEL Delete User with no Auth
   - DEL Delete User

## Expected vs Actual

**Expected:**  
- Auth requests succeed (`200`) and return the expected JSON.
- No-auth requests are rejected (`401`).
- Delete (auth) returns `{ "success": true }`.

**Actual:**  
1) **GET Me (auth):** `200 OK`, returned `user: { id, email, name, photoUrl }` ✅  
2) **GET Me With No Auth:** `401 Unauthorized` ✅  
3) **PUT Change User info (auth):** `200 OK`, returned updated `user` fields ✅  
4) **PUT Change User info With No Auth:** `401 Unauthorized` ✅  
5) **DEL Delete User With No Auth:** `401 Unauthorized` ✅  
6) **DEL Delete User (auth):** `200 OK`, returned `{ "success": true }` ✅  

## Outcome
**Pass/Fail:** Pass ✅  

## Evidence (logs / screenshots)
- Postman screenshots:
  - `docs/manual_tests/MT_001/GET-me.jpg`
  - `docs/manual_tests/MT_001/GET-me-no-auth.jpg`
  - `docs/manual_tests/MT_001/PUT-me.jpg`
  - `docs/manual_tests/MT_001/PUT-me-no-auth.jpg`
  - `docs/manual_tests/MT_001/DEL-me-no-auth.jpg`
  - `docs/manual_tests/MT_001/DEL-me.jpg`

## Next Steps (if needed)
- Nothing required (all checks passed).  
