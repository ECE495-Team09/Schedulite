# Manual Test Report

**Date (YYYY-MM-DD):** 2026-03-15  
**Tester:** Aryan Banerjee  

**Test Name:** Group CRUD ( Create, GET, PUT, DELETE) in Postman  

**Test Steps (summary):**
1. Started backend + DB, ensured a test user existed, and obtained a valid JWT for that user.
2. In Postman, set `Authorization: Bearer <JWT>` for the “auth” requests and removed auth for the “no auth” requests.
3. Ran these requests in order:
   - POST Create Group
   - Post Create Group with No Auth
   - GET Group by ID
   - GET Group by ID with No Auth
   - PUT Change Group info
   - PUT Change Group info with No Auth
   - DEL Delete Group with no Auth
   - DEL Delete Group

## Expected vs Actual

**Expected:**  
- Auth requests succeed (`200`) and return the expected JSON.
- No-auth requests are rejected (`401`).
- Delete (auth) returns `{ "success": true }`.

**Actual:**  
1) **POST Create Group (auth):** `200 OK`, returned `group: { id, name, description, members }` ✅
2) **GET Group by ID With No Auth:** `401 Unauthorized` ✅  
3) **PUT Change Group info (auth):** `200 OK`, returned updated `group` fields ✅  
4) **PUT Change Group info With No Auth:** `401 Unauthorized` ✅  
4) **GET Group by ID (auth):** `200 OK`, returned `group: { id, name, description, members }` ✅
5) **GET Group by ID With No Auth:** `401 Unauthorized` ✅
5) **DEL Delete Group With No Auth:** `401 Unauthorized` ✅  
6) **DEL Delete Group (auth):** `200 OK`, returned `{ "success": true }` ✅  

## Outcome
**Pass/Fail:** Pass ✅  

## Evidence (logs / screenshots)
- DB screenshots:
  - `docs/manual_tests/MT_002/POST-Group-Creation.jpg`
  - `docs/manual_tests/MT_002/POST-Group-DB.jpg`
  - `docs/manual_tests/MT_002/GET-Group.jpg`
  - `docs/manual_tests/MT_002/DELETE-Group.jpg`
  - `docs/manual_tests/MT_002/DELETE-Group-DB.jpg`
## Next Steps (if needed)
- Nothing required (all tests passed).  
