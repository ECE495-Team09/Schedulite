# Backend URL (must be reachable from the installed app).
# For EAS builds use a deployed URL, e.g. https://your-backend.railway.app
EXPO_PUBLIC_API_URL=http://localhost:5000

# Same Google OAuth Web client ID as the backend (required for Google Sign-In).
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
