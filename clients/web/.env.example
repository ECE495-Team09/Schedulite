# Backend API base URL. Use /api when running with Vite dev server (proxy to backend).
VITE_API_URL=/api

# Google OAuth client ID (same as backend GOOGLE_CLIENT_ID from Google Cloud Console).
# Create OAuth 2.0 credentials for "Web application" and add http://localhost:5173 to authorized origins.
VITE_GOOGLE_CLIENT_ID=test