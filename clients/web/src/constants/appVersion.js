/**
 * Root `version.json` (same source as backend `/health`); injected by Vite `define`.
 * @type {string}
 */
// eslint-disable-next-line no-undef -- replaced at build via vite.config define
export const APP_VERSION = __APP_VERSION__;
